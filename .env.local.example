# Copy to .env.local for local Docker (contact form / Resend).
# cp .env.local.example .env.local
# Edit RH_RESEND_API_KEY, then: docker compose up -d --force-recreate wordpress

RH_RESEND_API_KEY=re_your_resend_api_key_here
